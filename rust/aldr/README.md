Install rust for your operating system using the instructions
at https://www.rust-lang.org/tools/install.

To build the benchmark executable, run the following.

```sh
cargo build --release
```

The executable file is `./target/release/aldr`
